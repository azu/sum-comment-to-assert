{
  "directories": {
    "test": "test"
  },
  "author": "azu",
  "license": "MIT",
  "files": [
    "bin/",
    "lib/",
    "src/"
  ],
  "name": "sum-comment-to-assert",
  "version": "1.0.0",
  "description": "sum function",
  "main": "lib/sum-comment-to-assert.js",
  "scripts": {
    "test": "babel-node src/sum.js",
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build"
  },
  "keywords": [
    "jsdoc",
    "example"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/azu/sum-comment-to-assert.git"
  },
  "bugs": {
    "url": "https://github.com/azu/sum-comment-to-assert/issues"
  },
  "homepage": "https://github.com/azu/sum-comment-to-assert",
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-jsdoc-to-assert": "^4.0.0",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.14.0",
    "mocha": "^3.0.2",
    "power-assert": "^1.4.1"
  }
}